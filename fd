#!/bin/bash

if [ ! $1 ]; then
	echo "Find filename in current directory, need (multi) parameter."; exit
fi
#不搜索隐藏文件夹
cmd='find . ! -path "*/.*" '
for p in $*; do
cmd=$cmd"-iname \"*$p*\" "
done
echo -e "\e[1;34m>>>-----> $cmd\n\e[0m"
eval $cmd
