#[default]
# Write your default config options here!
vo=xv
#subcp=utf8
subcp=enca:zh:utf-8
# 此vf设置，有可以支持s热键实时截图
vf=screenshot
font="Vera Sans YuanTi Mono"
# 0 是精确匹配, 1是只要包含名称, 2 是所有字幕都加载
sub-fuzziness=1
#设置按什么方法来进行自动缩放字幕，0－不自动缩放，1－按电影高度缩放，2－按电影宽度缩放，3－按电影对角线缩放(默认值)
subfont-autoscale=1
#设置字幕文本的自动缩放系数(屏幕尺寸的百分比)，值范围为0～100，默认值为5。
subfont-text-scale=5
